module.exports = {
    // 是否在语句末尾添加分号
    semi: false,
    // 使用单引号而不是双引号
    singleQuote: true,
    // 每行的最大字符宽度
    printWidth: 100,
    // 每个缩进级别的空格数
    tabWidth: 4,
    // 是否使用制表符（tab）进行缩进
    useTabs: false,
    // 在对象和数组的最后一个元素后面添加逗号，'es5'表示只在 ES5 允许的地方添加（如对象、数组）
    trailingComma: 'es5',
    // 在对象字面量的花括号之间添加空格
    bracketSpacing: true,
}
